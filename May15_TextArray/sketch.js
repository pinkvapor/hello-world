var eyelash = {// was trying to make texts in an array to show up but it doesn't work the way I wanted it to
  wat("whatever"),
  who("So now"),
  uh("My dog is okay"),
  
}

function setup() {
  canvas(400, 400)
}

function draw() {
  eyelash.wat // I don't know what to do here
}